# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# 데이터를 쓸 때

## 숫자에는 기준이 붙는다

`minutes: 15, yen: 330` 만 적으면 그 숫자는 **아무 조건 없이 참인 값**으로 읽힌다.
실제로는 거의 항상 조건이 있다 — 케이큐선은 시나가와까지 15분 330엔이고
아사쿠사까지는 35분 599엔이다. 같은 노선, 다른 답이다.

그러니 숫자를 넣을 때마다 묻는다. **이 값은 무엇이 정해져야 참이 되나?**
도착지, 인원, 날짜, 방향, 성인/어린이. 답이 있으면 그것도 **같은 자료구조 안에
필드로** 넣는다. 주석이나 설명 문장에만 적으면 화면에는 안 나가고, 화면에
안 나가면 사용자에게는 없는 것이다.

기준이 여럿이면(리무진 버스가 우메다·난바·USJ 로 다 가고 요금이 셋 다 다르면)
숫자 하나로 뭉뚱그리지 말고 갈라 놓는다. 대표값 하나에 「등등」을 붙이는 건
나머지를 틀리게 말하는 것이다.

## 비교는 조건이 같을 때만

「가장 빠름 · 가장 저렴 · 추천」 같은 판단은 **비교 대상의 기준이 같은 자리**
에서만 계산한다. 도착지가 다른 두 노선의 소요시간을 나란히 놓고 앞의 것에
「가장 빠름」을 붙이면, 그 뱃지는 사용자를 틀린 선택으로 데려간다.

선택지가 하나뿐이면 「가장 ○○」을 아예 붙이지 않는다. 비교 대상이 없는데
최상급을 쓰면 다른 선택지가 있는 것처럼 읽힌다.

## 층을 얹을 땐 아래층을 먼저 본다

기존 화면 위에 새 안내를 덧붙일 때, **새로 쓰는 것보다 겹치는 것을 먼저 본다.**
같은 질문에 두 곳이 답하고 있으면 둘 중 하나는 반드시 틀리게 되고, 사용자는
어느 쪽을 믿을지 알 수 없다.

겹치면 셋 중 하나를 한다 — 역할을 나누거나(한쪽은 「얼마나 걸리나」, 다른 쪽은
「어떻게 타나」), 한쪽을 지우거나, 한쪽이 다른 쪽을 참조하게 한다.
**양쪽에 같은 값을 복사해 두는 선택지는 없다.**

기존 데이터를 안 건드리고 새 층만 얹는 건 작업을 끝낸 게 아니라 미룬 것이다.

## 확인하지 않은 값은 넣지 않는다

모르면 비워 두고 화면에서 그 자리를 지운다. 그럴듯한 값을 채우면 그게 확인된
값처럼 보인다. 확신이 반쯤이면 `confidence: 'approx'` 처럼 그 사실 자체를
데이터에 남기고, 화면에도 「약」과 재확인 안내를 함께 낸다.

내 기억 속 요금은 낡았다고 보는 편이 맞다. 2026년 3월 JR동일본·도쿄메트로,
2023년 10월 케이큐가 운임을 올렸다. **숫자를 쓰기 전에 찾아본다.**
