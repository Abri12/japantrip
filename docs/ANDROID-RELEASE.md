# 안드로이드 출시

> 이 문서는 **사람만 할 수 있는 일**을 적는다. 코드로 자동화할 수 있는 것은
> 코드에 있고, 여기 남는 것은 계정을 만들고 콘솔에서 클릭해야 하는 절차다.
> 절차를 기억에 두면 반드시 어긋난다 — 특히 1년에 한 번 하는 일은.

## 지금 어디까지 왔나

| # | 할 일 | 상태 |
|---|---|---|
| 1 | **FCM 자격증명** — 지진 알림 | 🟡 코드는 준비됨 · 콘솔 작업 남음 |
| 2 | **서버 배포 + HTTPS** | 🟡 설정 파일 준비됨 · VPS 작업 남음 |
| 3 | `expo-updates` (OTA) | 🟡 설치·설정됨 · `eas update:configure` 남음 |
| 4 | 스플래시·아이콘 교체 | ✅ |
| 5 | 실기기 훑기 (뒤로가기 · edge-to-edge) | 🟡 점검표 작성됨 · 기기 필요 |
| 6 | 위치 권한 고지 + Play 폼 · 알림 끄기 스위치 | ✅ |
| 7 | 스토어 등록물 | 🟡 문구 초안 있음 · 이미지 남음 |

---

# 1. FCM — 지진 알림이 실제로 가게 하기

## 왜 이게 1번인가

이 앱에서 **알림은 편의 기능이 아니라 안전 기능**이다. 긴급지진속보는 흔들림
도달까지 수 초~수십 초가 전부라, 앱을 열어 봐야 아는 정보는 쓸모가 없다.

그런데 이 경로는 **고장나도 아무 소리가 안 난다.** 빌드도 되고 심사도 통과하고
설치도 되는데 알림만 안 온다. 지진이 나야 알게 되고, 그때는 늦다.

## 무엇이 필요한가

Expo 푸시 서버는 안드로이드로 직접 못 보낸다. 구글의 FCM 이 유일한 통로다.

```
앱 → Expo 푸시 서버 → FCM → 기기
                       ↑
              여기를 통과하려면 자격증명이 필요하다
```

**두 개가 필요하고, 사는 곳이 다르다.**

| 무엇 | 어디에 두나 | 비밀인가 | 없으면 |
|---|---|---|---|
| `google-services.json` | 저장소 루트 · **커밋한다** | 아니다 (앱 안에 그대로 들어간다) | 빌드가 실패한다 |
| FCM V1 서비스 계정 키 (`.json`) | EAS credentials · **커밋 안 한다** | **그렇다** | 빌드는 되고 알림만 안 온다 |

> ⚠ 서비스 계정 키가 새면 **누구든 이 앱 이름으로 푸시를 보낼 수 있다.**
> `.gitignore` 에 `*-firebase-adminsdk-*.json` 등을 넣어 뒀지만, 파일 이름이
> 다르면 안 걸린다. EAS 에 올리고 나면 로컬 파일은 지우는 게 가장 안전하다.

## 절차

### 1-A. Expo 계정 · 프로젝트 연결

```bash
npx eas login      # 계정이 없으면 expo.dev 에서 먼저 만든다
npx eas init       # app.json 의 extra.eas.projectId 에 값을 적어 준다
```

`eas init` 이 만드는 `projectId` 가 **푸시 토큰을 받는 데 필요하다.**
이 값이 없으면 릴리스 빌드에서 토큰 요청이 던지고, 알림이 조용히 안 켜진다
(`src/lib/push.ts`). 개발 중에는 개발 서버가 대신 알려 줘서 티가 안 난다 —
**개발자에게만 잘 돌고 사용자에게만 안 도는** 종류의 고장이다.

빠졌으면 콘솔에 경고가 뜬다:

```
[push] EAS 프로젝트 id 가 없어서 지진 알림을 켤 수 없어요.
```

### 1-B. Firebase 프로젝트 · google-services.json

1. https://console.firebase.google.com → **프로젝트 추가**
   - 애널리틱스는 **끈다.** 이 앱은 사용 기록을 안 모은다(`PRIVACY.md`).
     켜면 데이터 세이프티 폼의 답이 통째로 달라진다.
2. **Android 앱 추가**
   - 패키지 이름: `com.hyun.japantrip` ← **정확히 이것.** 틀리면 조용히 안 온다
   - 닉네임·SHA-1 은 안 넣어도 된다 (푸시에는 필요 없다)
3. `google-services.json` 다운로드 → **저장소 루트에 둔다**
4. 커밋한다 — EAS 는 저장소에서 파일을 꺼내 쓰므로 안 올리면 빌드가 실패한다

`app.json` 은 이미 이 파일을 가리키고 있다:

```json
"android": { "googleServicesFile": "./google-services.json" }
```

> 파일이 없으면 `expo prebuild` / EAS 빌드가 **명확한 에러로 멈춘다.**
> 조용히 넘어가는 것보다 낫다 — 이 자리는 조용히 넘어가면 안 되는 자리다.

### 1-C. 서비스 계정 키를 EAS 에 올리기

1. Firebase Console → ⚙ **프로젝트 설정** → **서비스 계정** 탭
2. **새 비공개 키 생성** → JSON 파일이 받아진다
3. 올린다:

```bash
npx eas credentials
#   platform         → Android
#   profile          → production
#   what to do       → Google Service Account
#                    → Manage your Google Service Account Key for Push Notifications (FCM V1)
#                    → Set up a Google Service Account Key
#   경로를 묻거든 방금 받은 JSON 을 가리킨다
```

4. **로컬 JSON 파일을 지운다.** EAS 에 올라갔으면 더 필요 없다

## 확인 — 실제로 오는지

여기까지 하고 나면 **반드시 실기기에서 확인한다.** 이 경로는 시뮬레이터로도
웹으로도 검증되지 않는다(`Device.isDevice` 에서 끊긴다).

```bash
npx eas build -p android --profile preview   # APK 가 나온다
```

1. APK 를 실기기에 설치
2. 도시를 하나 고른다 → 알림 권한 창이 뜨는지
3. 서버 명부에 토큰이 들어왔는지 확인 (`.data/subscribers.json`)
4. Expo 푸시 도구로 한 발 쏴 본다 — https://expo.dev/notifications
   - `ExponentPushToken[...]` 을 붙여넣고 전송
   - **소리와 함께 뜨는지**, 잠금화면에서도 뜨는지 (`eew` 채널은 `bypassDnd`)

3번까지만 되고 4번이 안 되면 **FCM 자격증명 문제**다. 3번이 안 되면
`projectId` 나 권한 문제다 — 이 둘은 증상이 같아서 순서대로 갈라야 한다.

> ⚠ **2번(서버 배포)이 안 끝나면 3·4번을 못 한다.** 등록할 서버가 없으면
> `registerQuakePush` 가 권한도 묻지 않고 나간다. FCM 설정만으로는 여기까지가
> 끝이고, 진짜 확인은 2번 뒤에 한 번 더 해야 한다.

## ~~아직 안 된 것 — 알림을 끌 방법이 없다~~ (6번에서 해결)

`unregisterQuakePush()` 는 **아무 데서도 안 불린다.** 화면에 알림을 끄는
스위치가 없다.

그래서 지금 두 가지가 사실과 다르다:

- 시스템 설정에서 알림을 꺼도 **서버 명부에서는 토큰이 안 빠진다.** 계속 보내고
  기기가 버린다
- `docs/PLAY-DATA-SAFETY.md` 의 「사용자가 삭제를 요청할 수 있나 → 예, 알림은
  끄면 지워진다」가 **현재는 거짓**이다

출시 전에 스위치를 만들거나, 문서의 답을 고쳐야 한다. 스위치를 만드는 쪽이 맞다.

**6번에서 같이 하기로 했다**(2026-08-22). 위치 권한 고지 문구와 같은 성질의 일이고
— 둘 다 「사용자에게 무엇을 언제 묻고, 어떻게 무를 수 있게 하나」 — Play 폼을
채우는 자리에서 함께 봐야 답이 어긋나지 않는다.

같이 볼 것이 하나 더 있다. 지금은 **도시를 고르는 순간 알림 권한 창이 뜬다**
(`selected-city.tsx`). 사용자는 왜 뜨는지 모른 채 창을 만난다. 스위치를 만들면
그 스위치가 묻는 자리가 되므로, 이 문제도 같이 사라진다.

---

# 2. 서버 배포 — HTTPS 로 띄우기

## 왜 이게 2번인가

앱은 서버가 없어도 죽지 않는다(`src/lib/api.ts`). 하지만 **없으면 조용히 사라지는
것들**이 있다.

| 기능 | 서버 없으면 |
|---|---|
| 지진 푸시 | 등록 자체가 안 된다 — 권한도 안 묻는다 |
| 운행정보 (ODPT · JR서일본) | 없음 |
| 시간당 환율 | 하루 1회 소스로 떨어짐 |
| 리뷰 · 신고 | 전부 불가 |
| 크래시 리포트 | **앱이 죽어도 모른다** |

그리고 **1번(FCM)의 확인이 여기 걸려 있다.** 등록할 서버가 없으면 알림이 실제로
오는지 볼 방법이 없다.

## 이 서버가 요구하는 것 — 선택지가 좁은 이유

```
의존성 0 · Node 22 내장 http/fetch      → 빌드 단계가 없다. git pull 이면 끝
파일 기반 저장 (원자적 rename)          → 영구 디스크가 필요하다
P2PQuake WebSocket 상주 · IP당 동시 2연결 → 서버리스 불가 · 인스턴스 하나만
HTTPS                                    → 데이터 세이프티 답이 여기 걸린다
```

WebSocket 을 계속 물고 있어야 해서 **Cloudflare Workers · Vercel 같은 서버리스가
전부 탈락**한다. 무료 티어의 sleep 도 안 된다 — 자는 동안 지진을 놓친다.

인스턴스를 둘 이상 띄우게 되면 하나만 `QUAKE_WATCH=on` 으로 두고 나머지는 꺼야
한다. P2PQuake 가 IP당 동시 2연결이라 그냥 늘리면 전부 막힌다.

## ⚠ 주소 문제 — VPS 는 이름을 안 준다

**VPS 는 IP 만 준다.** `fly.dev` 나 `onrender.com` 처럼 딸려 오는 주소가 없고,
Let's Encrypt 는 **IP 주소에 인증서를 안 내준다.** 이름이 어디선가는 와야 한다.

| 방법 | 비용 | 문제 |
|---|---|---|
| **DuckDNS** (`japantrip.duckdns.org`) | 무료 | 없음. 공개 접미사 목록에 있어서 인증서 발급 한도를 남과 안 나눈다 |
| `sslip.io` / `nip.io` (`1-2-3-4.sslip.io`) | 무료 · 가입 없음 | 공개 접미사 목록에 **없다.** 한도를 전 세계 사용자와 나눠 써서 「이미 너무 많이 발급됨」으로 막힐 수 있다 |
| 도메인 구입 | 연 1~2만원 | 없음. 호스팅을 옮겨도 앱 안의 주소가 안 바뀐다 |

**DuckDNS 로 시작하고, 나중에 도메인을 사면 그때 옮기는 것**을 권한다. 다만 옮기는
순간 앱을 새로 배포해야 한다 — `EXPO_PUBLIC_API_BASE` 는 **번들에 박히는 값**이라
서버 주소를 바꾸면 스토어 심사를 다시 거친다. 3번(OTA)이 없는 동안에는 이게 아프다.

> 이 순서가 뜻하는 것: **3번을 먼저 하면 주소를 나중에 바꿔도 싸진다.**
> 도메인을 안 살 거라면 3번을 2번보다 먼저 하는 게 나을 수 있다.

## 절차

### 2-A. VPS 와 이름

1. VPS 를 만든다 (Hetzner CX22 · Vultr · Oracle Cloud 평생 무료 ARM 중 아무거나)
   - **Ubuntu 24.04 LTS**, 1vCPU / 1GB 면 충분하다. 이 서버는 캐시 몇 개와
     WebSocket 하나가 전부다
2. https://www.duckdns.org 에서 이름 하나 만들고 VPS 의 IP 를 넣는다
3. 방화벽은 **80 · 443 만** 연다. 8787 은 절대 열지 않는다 — 프록시를 건너뛰는
   경로가 되고, 거기엔 TLS 도 없다

### 2-B. Node 22 · 사용자 · 코드

```bash
# Node 22 (Ubuntu 기본 저장소는 낡았다)
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt install -y nodejs git

# 앱 전용 사용자 — 로그인 못 하게
sudo useradd --system --home /srv/japantrip --shell /usr/sbin/nologin japantrip
sudo mkdir -p /srv/japantrip
sudo chown japantrip:japantrip /srv/japantrip

sudo -u japantrip git clone https://github.com/Abri12/japantrip.git /srv/japantrip
sudo -u japantrip mkdir -p /srv/japantrip/.data
```

`npm install` 이 없다. 이 서버는 **의존성이 0** 이라 `node server/index.mjs` 로 바로 뜬다.

### 2-C. `.env`

`/srv/japantrip/.env` 에 둔다. 이 파일 하나가 유일한 설정 자리다.

```bash
PORT=8787

# 바깥에 열지 않는다. 프록시가 같은 기계에 있다
HOST=127.0.0.1

# 프록시 뒤에 있다고 알려 준다.
# 이게 꺼져 있으면 모든 요청이 127.0.0.1 에서 온 것으로 보여
# 요청 제한이 통째로 한 양동이가 된다 — 한 명이 많이 쓰면 전부 막힌다.
TRUST_PROXY=1

# 웹 버전(GitHub Pages)이 이 서버를 부를 수 있게. 네이티브 앱은 CORS 와 무관하다
ALLOWED_ORIGIN=https://abri12.github.io

QUAKE_WATCH=on
SUBSCRIBERS_FILE=/srv/japantrip/.data/subscribers.json

# 있으면 환율이 시간당 갱신된다. 없어도 동작한다
OPEN_EXCHANGE_RATES_APP_ID=
# 도쿄메트로·JR동일본 운행정보까지 넓히려면 (무료·상업 이용 가능)
ODPT_TOKEN=
```

```bash
sudo chown japantrip:japantrip /srv/japantrip/.env
sudo chmod 600 /srv/japantrip/.env
```

### 2-D. systemd

```bash
sudo cp /srv/japantrip/deploy/japantrip.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now japantrip
sudo systemctl status japantrip
curl -s localhost:8787/api/fx | head -c 200      # 여기서 JSON 이 나와야 한다
```

### 2-E. Caddy

```bash
sudo apt install -y caddy
sudo cp /srv/japantrip/deploy/Caddyfile /etc/caddy/Caddyfile
sudo nano /etc/caddy/Caddyfile        # 도메인을 실제 주소로
sudo systemctl reload caddy
```

인증서는 자동으로 받는다. 확인:

```bash
curl -s https://japantrip.duckdns.org/api/fx | head -c 200
```

### 2-F. 앱이 서버를 보게 하기

`.env` (저장소 루트, 앱 쪽):

```
EXPO_PUBLIC_API_BASE=https://japantrip.duckdns.org
```

> ⚠ **이 값은 번들에 박힌다.** 앱을 다시 빌드해야 반영된다. 그리고 `.env` 는
> `.gitignore` 에 있으므로 EAS 빌드에서는 이 값을 따로 넣어야 한다:
> `eas env:create --name EXPO_PUBLIC_API_BASE --value https://... --scope project`

## 배포한 뒤 — 코드를 고쳤을 때

```bash
sudo -u japantrip git -C /srv/japantrip pull
sudo systemctl restart japantrip
```

두 줄이 전부다. 빌드도 `npm install` 도 없다.

## 확인 — 프록시 뒤에서 진짜로 도는지

```bash
# ① 지어낸 헤더가 안 먹는지. 두 번 다 같은 양동이여야 한다
for i in 1 2; do curl -s -o /dev/null -w "%{http_code}\n" \
  -H "X-Forwarded-For: 9.9.9.$i" https://japantrip.duckdns.org/api/fx; done

# ② 8787 이 바깥에서 안 열리는지 — 연결이 거부돼야 정상이다
curl -m 5 http://<VPS-IP>:8787/api/fx

# ③ 지진 감시가 붙었는지
sudo journalctl -u japantrip -n 50 | grep -i quake
```

②가 응답하면 **방화벽이나 `HOST` 설정이 잘못된 것**이다. 그 경로로는 TLS 없이
평문이 오가고, `x-forwarded-for` 를 마음대로 적어 보낼 수 있다.

## 그리고 1번으로 돌아간다

서버가 뜨면 **FCM 확인을 마저 한다** (§1 「확인 — 실제로 오는지」의 3·4번).
여기까지 와야 지진 알림이 실제로 도는 것을 처음 보게 된다.

---

# 3. OTA — 데이터를 고칠 방법 만들기

## 왜 필요한가

이 앱의 데이터는 **전부 번들 안에 있다.** 요금 하나, 영업시간 하나를 고치려면
스토어 심사를 다시 거친다 — 1~7일.

그런데 이 프로젝트는 **월간 OSM 대조 감사**(`.github/workflows/data-audit.yml`)를
돌려 드리프트를 잡아낸다. 잡아내고도 반영을 못 하는 구조다.
`docs/SERVER.md` 6번에도 같은 문제가 미해결로 적혀 있다.

여행 앱에서 이건 단순한 불편이 아니다. **틀린 요금을 보고 표를 사는 사람**이
생기고, 우리는 그걸 알면서 일주일을 기다린다.

## 해 둔 것

```jsonc
// app.json
"runtimeVersion": { "policy": "appVersion" },
"updates": {
  "enabled": true,
  "checkAutomatically": "ON_LOAD",
  "fallbackToCacheTimeout": 0
}
```

**`fallbackToCacheTimeout: 0` 이 이 설정에서 가장 중요하다.** 앱을 켤 때
업데이트를 **기다리지 않는다.** 받아 두고 다음 실행에 적용한다.

기다리게 하면 어떻게 되는지 생각해 보면 답이 하나뿐이다 — 여행지의 네트워크는
느리고 자주 끊긴다. 공항에 막 내려서 앱을 켰는데 스플래시에서 몇 초를 붙잡히면,
그건 우리가 고친 요금보다 훨씬 큰 손해다.

**`runtimeVersion: appVersion`** — 네이티브가 바뀌지 않은 한 같은 버전 안에서
데이터·화면 수정을 밀어 넣을 수 있다. 네이티브 의존성을 바꾸면 `version` 을
올리고 스토어를 거쳐야 한다. 그게 맞다: 새 네이티브 코드를 옛 앱에 밀어 넣으면
그 자리에서 죽는다.

## 남은 것 — `eas init` 뒤에

```bash
npx eas update:configure    # app.json 에 updates.url 을 적어 준다
```

지금은 `url` 이 비어 있어서 **업데이트를 확인할 곳이 없다.** 설정은 다 돼 있고
주소만 빈 상태다. `eas init`(§1-A)을 하고 나면 위 한 줄로 채워진다.

밀어 넣을 때:

```bash
npx eas update --branch production --message "삿포로 지하철 요금 인상 반영"
```

## 주의 — 데이터 요금

`ON_LOAD` 는 앱을 켤 때마다 확인한다. 여행자는 **국제 로밍**을 쓰는 경우가
많아서, 번들을 내려받는 만큼 데이터를 쓴다.

`"checkAutomatically": "WIFI_ONLY"` 로 바꾸면 그 비용이 사라지는 대신,
**호텔 와이파이에 안 붙는 사람에게는 수정이 영영 안 간다.** 이 기능이
존재하는 이유가 「여행 중에 틀린 요금을 고치는 것」이라 그쪽을 막으면 앞뒤가
안 맞는다. 지금은 `ON_LOAD` 로 두되, 번들이 커지면 다시 볼 일이다.

---

# 4. 아이콘 ✅

넉 장을 후지산 마크로 갈아끼웠다(`92d7af9`). 템플릿 잔여 에셋 15개도 지웠다.

> **이 마크는 자리를 지키는 것이다.** Expo 로고보다는 낫지만 디자이너의 것은
> 아니다. 갈아끼우려면 `assets/images` 의 넉 장만 바꾸면 된다 —
> `icon.png`(1024) · `android-icon-foreground.png`(1024, 안전지대 넉넉히) ·
> `android-icon-monochrome.png`(알파만) · `splash-icon.png`.

---

# 5. 실기기 점검표

## 왜 이 목록인가

**검증이 전부 브라우저에서 이뤄졌다.** `check:screens` 는 내보낸 웹 HTML 을
읽고, 테스트 230개는 Node 에서 돈다. **안드로이드에서 도는 검사는 0개다.**

`Platform.OS` 분기가 다섯 곳뿐이라 표면적은 작다. 문제는 분기가 아니라
**웹에 아예 없는 것들**이다 — 상태바, 시스템 네비게이션 바, 하드웨어 뒤로가기,
네이티브 탭바, TTS. 이건 코드를 아무리 읽어도 안 보인다.

아래는 **코드를 읽어서 「여기가 다를 것」이라고 지목한 자리**다. 기기가 생기면
이 순서로 훑는다.

### ① 시스템 바 — edge-to-edge

`app.json` 에 `edgeToEdgeEnabled: true` 라 화면이 상태바·네비게이션 바 **뒤까지**
그려진다.

- [ ] 상태바 글자가 밝은 테마·어두운 테마에서 **각각 보이는가** (`StatusBar style="auto"` 를 넣었지만 확인은 기기에서)
- [ ] 화면 맨 위 제목이 상태바에 가리지 않는가 (`screen.tsx` 가 `insets.top` 을 준다)
- [ ] **제스처 네비게이션과 3버튼 네비게이션 양쪽**에서 맨 아래 내용이 안 가리는가

③번이 가장 의심스러운 자리다. `constants/theme.ts` 의 `BottomTabInset = 84` 가
**하드코딩**인데, 3버튼 네비게이션은 제스처보다 약 24dp 더 높다. 스크롤 맨
아래 항목이 탭바에 반쯤 덮일 수 있다.

> `screen.tsx` 의 footer 는 `insets.bottom` 을 제대로 쓰는데, 스크롤 패딩만
> 상수를 쓴다. **한 파일 안에서 두 방식이 섞여 있다** — 둘 중 하나는 틀렸다.

### ② 하드웨어·제스처 뒤로가기

`BackHandler` 를 쓰는 곳이 **하나도 없다.** 화면 이동은 expo-router 가 알아서
처리하므로 대개 맞지만, 웹에서는 브라우저 뒤로가기가 대신해 줘서 **한 번도
확인된 적이 없는 경로**다.

- [ ] 상세 화면(장소·공항·코스)에서 뒤로가기 → 목록으로 오는가
- [ ] 탭을 옮긴 뒤 뒤로가기 → 앱이 바로 꺼지지 않는가
- [ ] 「내 일정」의 쓴 돈 칸에서 **입력 중**에 뒤로가기 → 적던 값이 날아가지 않는가

### ③ 네이티브 탭바

`expo-router/unstable-native-tabs` 를 쓴다. **이름에 unstable 이 들어 있다.**

- [ ] 탭 다섯 개의 Material 아이콘이 전부 나오는가 (`md="home|flight_land|train|map|notifications"`)
- [ ] 선택된 탭 색이 팔레트와 맞는가
- [ ] 탭을 눌렀을 때 스크롤이 맨 위로 가는가

### ④ 알림 (2번 뒤에)

`push.ts` 는 네이티브 모듈을 부르므로 시험을 못 붙인다. 아래가 유일한 검증이다.

- [ ] 도시를 골라도 권한 창이 **안 뜨는가** (예전에는 떴다)
- [ ] 안전 탭의 스위치를 켜면 그때 권한 창이 뜨는가
- [ ] 설정 → 앱 → 알림에 채널이 **둘** 보이는가 (「긴급지진속보」·「지진 정보」)
- [ ] Expo 푸시 도구로 쏜 알림이 **잠금화면에서 소리와 함께** 뜨는가
- [ ] **앱을 완전히 껐다 켠 뒤** 스위치를 끄면 서버 명부(`.data/subscribers.json`)에서 토큰이 빠지는가 — 여기가 고장나 있던 자리다
- [ ] 껐다가 다시 켜면 다시 들어오는가

### ⑤ 나머지 — 웹에서 안 드러나는 것들

- [ ] **말하기**(`etiquette.tsx`, expo-speech) — 일본어 문구가 실제로 읽히는가. 안드로이드는 일본어 TTS 데이터가 없으면 조용히 아무 일도 안 한다
- [ ] **전화 걸기**(`safety/actions.ts`, `tel:`) — 다이얼러가 열리는가
- [ ] **지도 열기**(`Linking.openURL`) — 구글 지도 앱으로 넘어가는가, 웹으로 떨어지는가
- [ ] **폰트** — Pretendard 가 실제로 적용되는가 (네이티브는 번들 TTF, 웹은 CSS 라 경로가 다르다)
- [ ] **위치 권한 거부** — 거부해도 다른 화면이 전부 도는가
- [ ] **비행기 모드** — 날씨·환율이 실패해도 화면이 안 죽는가

### ⑥ 되돌아가서

- [ ] 앱을 지웠다 다시 깔았을 때 저장·일정·쓴 돈이 **전부 사라지는가** (`PRIVACY.md` 가 그렇게 약속한다)

---

# 7. 스토어 등록물

## 문구 — 그대로 붙여 넣으면 되는 것

### 앱 이름 (30자)

```
일본 여행 안전 가이드
```

### 짧은 설명 (80자)

```
공항에서 시내까지, 요금과 시간을 확인한 날짜까지 밝히는 일본 여행 안내
```

### 전체 설명 (4000자)

```
일본 여행, 검색창 열 일 없이.

간사이 공항 도착 로비에서 난카이 라피트와 공항급행 중 뭘 타야 할지 몰라
블로그를 세 개 열어보고, 결국 2019년 글을 보고 표를 삽니다. 요금은 그 사이
올랐습니다.

이 앱은 그 20분을 없애려고 만들었습니다.


■ 공항에서 시내까지, 출구 번호부터

공항 9곳 · 27개 경로를 정리했습니다. 몇 번 출구로 나가서, 무슨 표지판을 보고,
몇 분을 걷는지까지 순서대로 알려드립니다. 표지판은 일본어 원문을 그대로
병기해서 — 하카타역(博多駅) — 역에서 눈으로 대조만 하면 됩니다.

갈아타는 횟수, 캐리어 둘 자리, 실제로 붐비는 구간까지 비교합니다.


■ 돌아가는 날, 전철이 다니는지까지

여행 앱들이 이상하게 조용해지는 구간입니다. 새벽 6시 비행기면 몇 시에 나서야
할까요. 비행기 시각만 고르면 계산해 드리고, 그 시각에 첫차가 있는지까지 봅니다.
없으면 없다고 말합니다.


■ 오늘 나가도 되는지부터

날씨 아이콘과 기온만으로는 나가도 되는지 모릅니다. 일본 환경성 열중증 기준에
맞춰 한 줄을 더 붙였습니다. 태풍철 기상특보와 지진도 같은 카드 안에 있습니다.

지진 알림을 켜면 머무는 지역에 진도 4 이상이 오면 알려드립니다.


■ 관광지 175곳, 도시 10곳

도쿄 · 오사카 · 교토 · 후쿠오카 · 삿포로 · 나고야 · 오키나와 · 시즈오카 ·
다카마쓰 · 마쓰야마.

입장료는 원화로 함께 보여주고, 담아둔 곳으로 하루 최소 비용을 냅니다.
쓴 돈은 하루에 한 줄로 적을 수 있습니다 — 분류도 영수증도 없습니다.


■ 확인한 날짜를 밝힙니다

여행 정보가 틀리는 이유는 대부분 「낡아서」입니다. 그래서 요금과 영업시간에
확인한 날짜를 함께 적었습니다. 확인이 안 된 값은 그럴듯하게 채우지 않고
비워 둡니다. 좌표는 OpenStreetMap 과 매달 자동으로 대조합니다.


■ 회원가입이 없습니다

이메일도 전화번호도 묻지 않습니다. 광고 SDK와 분석 SDK를 하나도 쓰지 않고,
검색어나 화면 이동 기록을 모으지 않습니다.

저장한 장소, 일정, 쓴 돈은 전부 이 기기 안에만 있습니다. 앱을 지우면 함께
사라집니다.


개인정보처리방침: https://abri12.github.io/japantrip/privacy
지도 데이터: © OpenStreetMap 기여자 (ODbL)
```

### 카테고리 · 분류

| 항목 | 답 |
|---|---|
| 카테고리 | 여행 및 지역정보 |
| 태그 | 여행 가이드 · 교통 |
| 광고 포함 | **아니오** |
| 인앱 구매 | **아니오** |
| 개인정보처리방침 URL | `https://abri12.github.io/japantrip/privacy` |

개인정보처리방침은 **이미 배포돼 있다** — 웹 빌드가 `dist/privacy.html` 을
만들고 GitHub Pages 가 올린다. 따로 만들 것이 없다.

### 콘텐츠 등급 설문

전부 **아니오**다. 폭력·성적 내용·욕설·약물·도박 없음. 사용자 간 소통은
**리뷰 하나**뿐이고, 신고와 차단이 둘 다 있다(등급 설문에서 이걸 묻는다).

### 데이터 세이프티

`docs/PLAY-DATA-SAFETY.md` 에 **폼 그대로 옮겨 적을 수 있게** 정리돼 있다.
코드를 읽어서 만든 문서라 폼을 채우기 전에 그것부터 본다.

> ⚠ 「전송 중 암호화」는 **2번(HTTPS)이 끝나야 참이 된다.** 코드가 보장해
> 주지 않는 유일한 항목이다.

## 이미지 — 아직 없는 것

| 무엇 | 규격 | 상태 |
|---|---|---|
| 앱 아이콘 | 512×512 PNG | ✅ `assets/images/icon.png` 를 줄여서 |
| **피처 그래픽** | **1024×500** PNG/JPG | ❌ 필수인데 없다 |
| **스크린샷 (폰)** | 최소 2장 · 16:9 또는 9:16 · 320~3840px | ❌ |
| 스크린샷 (7·10인치) | 선택 | — |

**피처 그래픽은 없으면 등록이 안 된다.** 스토어 상단에 뜨는 가로 배너다.

스크린샷은 **실기기에서 찍는 게 맞다**(5번과 같이 하면 된다). 웹 화면을 찍어
올리면 상태바·탭바가 실제와 달라서, 받은 사람이 보는 것과 다른 그림이 된다.

찍을 화면은 이 넷을 권한다 — 앱이 남과 다른 자리들이다:

1. **공항 → 시내 카드** (출구 번호·표지판 일본어 병기가 보이는 곳)
2. **홈 날씨 위젯** (열중증 판정 한 줄이 보이는 상태)
3. **장소 상세** (확인한 날짜 + 원화 환산이 보이는 곳)
4. **돌아가는 날** (첫차가 없다고 말하는 화면 — 이게 제일 다르다)

---

# 6. 알림 스위치 · 위치 권한 ✅

## 위치 고지는 이미 있었다

처음 점검에서 「인앱 고지 문구가 없다」고 적었는데 **틀렸다.**
`features/place/review-form-section.tsx` 가 권한을 요청하는 버튼 **위에**
고지를 그리고 있고, 코드 주석이 그 이유까지 적어 두었다.

내용도 정직하다 — 「저장하지 않아요」로 끝내지 않고 **위치 위조를 막으려고
마지막 좌표 한 건을 하루 동안 둔다**는 것까지 밝힌다. 그 한 건을 안 적으면
고지가 거짓이 된다.

그리고 **Play 의 권한 선언 폼은 필요 없다.** 그 폼은 배경 위치
(`ACCESS_BACKGROUND_LOCATION`)를 요구하는 앱에만 해당하는데, 이 앱은 전경
위치만 쓴다. 자세한 근거는 `docs/PLAY-DATA-SAFETY.md` 의 「위치 — 폼이 하나 더
필요한가」에 적었다.

## 알림 스위치 — 세 문제가 한꺼번에

스위치를 만들려고 코드를 열었더니 문제가 셋이었다.

### ① 끄기가 앱을 다시 켜면 고장나 있었다

`unregisterQuakePush()` 는 메모리에 있는 토큰을 썼다. 앱을 껐다 켜면 그 값이
비어 있어서 **아무것도 안 보내고 조용히 끝난다.** 화면에는 꺼진 것으로 보이는데
서버 명부에는 토큰이 그대로 남는다.

그런데 끄는 시점은 **대부분 앱을 다시 켠 뒤**다. 즉 끄기는 거의 항상
실패했을 것이다. 토큰을 저장소에 남겨서 고쳤다.

### ② 도시를 고르면 권한 창이 떴다

`selected-city.tsx` 가 도시가 정해질 때마다 `registerQuakePush` 를 불렀다.
사용자는 도시를 골랐을 뿐인데 알림 권한 창을 만난다.

**한 번 거절하면 시스템이 다시 묻지 않는다.** 나중에 켜고 싶어져도 설정 앱까지
들어가야 하고, 대부분은 거기서 잃는다. 묻기에 가장 나쁜 순간에 묻고 있었던
셈이다.

이제 도시 변경은 `refreshQuakePush()` 를 부르고, 그건 **켜 둔 사람의 체류 현만
갱신한다.** 권한을 묻는 자리는 스위치 하나뿐이다.

### ③ 개인정보처리방침이 거짓이었다

`PRIVACY.md` 와 데이터 세이프티 문서가 「알림은 끄면 지워진다」고 적고 있는데
끌 방법이 없었다. 스위치가 생기면서 그 문장이 참이 됐다.

## 어디에 뒀나

안전 탭의 **긴급지진속보 바로 아래**다. 설정 화면을 따로 만들지 않았다 —
켜고 끌 것이 이것 하나뿐인데 화면을 열면 「찾아 들어가야 하는 자리」가 생긴다.

이 알림이 실어 나르는 것이 바로 위 구역의 내용이라, 「지금은 조용해요」를 읽은
사람이 이어서 「흔들리면 알림으로 받을까」를 생각한다. 생각이 난 자리와 스위치가
같은 자리에 있다.

켜기 전에 무엇이 서버로 가는지 밝힌다 — 위치 인증과 같은 규칙이다.

> **시험이 없다.** `push.ts` 는 expo 네이티브 모듈을 부르므로 Node 시험
> 환경에서 불러올 수 없다(앱 시험 82개는 전부 순수 함수·데이터다). 이 세
> 가지는 **5번 실기기 점검에서 눈으로 확인해야 한다** — 점검표 ④에 넣었다.
